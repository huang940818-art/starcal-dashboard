# 星歷儀表板

個人儀表板：記帳、待辦與行程、備忘錄、想法牆。

**線上試用 → https://huang940818-art.github.io/starcal-dashboard/**

打開就能直接玩（裡面是編出來的示範資料）。想拿來記自己的東西，
點右上角的徽章 →「清空，我要自己用」。

---

## 為什麼做這個

我自己寫了一支 iOS App 管健康、記帳、待辦。但有兩件事手機做不好：

1. **記帳只看得到「花了多少」，看不到「還剩多少」。** 沒有預算、沒有跨月趨勢、
   翻不到「上次那家店花多少」。
2. **想法沒有地方放。** 清單會強迫你先決定順序，但想法還沒成形的時候
   根本不知道哪個該排前面。

這個儀表板補的就是這兩塊，跑在電腦上。

## 有什麼

### 記帳

| | |
|---|---|
| **預算與超支** | 每個月給各分類設上限，看得到還剩多少、快超了會變色 |
| **跨月趨勢** | 近 6／12 個月或今年的收支長條圖，加一句話總結 |
| **月份切換** | 上一個月、這個月都能翻。往回會停在最早有帳目的月份 |
| **搜尋與篩選** | 關鍵字 × 收支別 × 分類 × 帳戶 × 期間，篩完的結果自己就是答案 |
| **固定 vs 彈性** | 分類標上性質，才知道真正能省的有多少 |
| **自訂分類** | 分類不寫死。寫死的分類遲早會缺一個，缺了就只能記到「其他」 |

帳戶餘額、轉帳、訂閱（週期換算成每月、下次扣款）也都有。

### 接下來（行程 ＋ 待辦 ＋ 課表）

**行程和待辦排在同一條時間線上**，因為它們是同一件事的兩個面向。
「明天要交回函」和「明天下午開會」都是明天要處理的事，分兩個分頁看，
就得自己在腦子裡合併——那正是最容易漏掉東西的地方。

同一份資料有三個檢視，各自回答一個問題：

| | 回答的問題 |
|---|---|
| **時間線**（預設） | 接下來要做什麼 |
| **月曆** | 這個月長什麼樣 |
| **課表** | 這禮拜哪些時段被佔走了 |

月曆格子裡的每一件事**都可以直接點開來改**，左邊一條分類顏色的線。
原本要先點格子選日期、再到底下的清單裡找同一件事點第二次——
眼睛已經看到它了，卻不能直接動它。

**月曆刻意不是預設檢視。** 行程不密集的話，空月曆每次打開都在說
「你什麼都沒有」——那會變成另一個讓人覺得自己沒做好的東西。
時間線問的是「接下來」，那才是打開這一頁的原因。

**課表照節次排，因為學校的課表就是照節次排的。**
學校發下來的是「第 9-10 節」，不是「9:10-12:00」。要人自己把節次換算成
時間再填進去，是把學校的問題丟給使用者。節次對應幾點可以自己設，
**留白也完全能用**——課表本來就是靠節次讀的。

**空的格子可以直接點。** 要人先按「加一堂」再從頭選星期和節次，
等於把眼睛已經看到的資訊再用手輸入一次。

**完全沒課的連續節次會收起來。** 課通常擠在一天的某幾節，
前面整片空白全部畫出來的話，得捲過一大片什麼都沒有的格子才看得到重點。

打工班表沒有節次，所以另有一種「照時間」的模式，建課表的時候選一次。
可以存好幾份——這學期、下學期、打工班表——切一下換整份，舊的留著，
也能照著複製一份再改。

課會出現在時間線上，但**樣式比行程輕，而且不算進「今天有幾件事」**：
課表是每週固定的，每天四五堂加進去的話那個數字永遠是兩位數，
「今天其實只有三件事」這個訊息就消失了。

**分類**：行程、待辦、課共用一組分類（名字和顏色都能改），
每一列前面一個色點，上面一排可以點著只看某一類。

### 備忘、想法牆

- **備忘**：跟待辦刻意分開。備忘沒有「完成」這個概念，硬塞進待辦只會讓待辦看起來做不完。
- **想法牆**：可以拖的便利貼。清單會強迫你先決定順序，但空間關係本身就是一種思考——
  把相關的擺一起、把還不確定的推到角落。顏色可以用色票，也可以一張一張自己挑。

### 外觀

八個主題，兩個是亮色的。**主題是一整組配色**——背景、卡片、分隔線、
三層文字、主色，加上七個分類色。第一版只換了 `--accent` 一個變數，
換完幾乎看不出差別，等於沒有這個功能。

亮色主題不換分類色的話，那些為深底調的粉彩在白紙上會淡到看不見——
「換個主題就有一半的東西不見了」比不給換更糟。所以每個主題的**每一種顏色
都驗過對比度**（`檢查.mjs` 裡有一條專門跑這件事），不是挑一個看起來對就放行。

幾組配色有來歷（Tokyo Night、Catppuccin、Rosé Pine、Gruvbox）——
那些是很多人盯過很久的配色，對比和層次都站得住。預設那組「星歷」
是從 iOS 星歷的 App icon 長出來的。

**磨砂玻璃**是另一個開關：卡片變半透明，背後透出幾團跟著主題走的色光。
玻璃要有東西可以透才成立，純色背景上做模糊等於什麼都沒做。
只套在大塊上——月曆一頁三十五格，每格都掛 `backdrop-filter` 會直接卡住。

主色可以在主題之上再換掉。**主色上面的文字色是算出來的，不是寫死的**——
用「亮度超過某個門檻就配深字」那種判斷會在中間調挑錯（例如 `#7FB4E8` 的亮度
只有 0.43，看起來「不夠亮」，但它配深字的對比度是 6.3，配淺字只有 1.9）。
直接算兩邊的對比度取高的，就沒有需要猜的數字。

## 幾個設計決定

**同一份前端跑兩種模式。**
偵測得到本機 server 就用真實資料（存成檔案）；偵測不到（例如放在 GitHub Pages 上）
就切成展示模式，用內建示範資料 + `localStorage`。所以線上版打得開、可以點，
而私人資料從來不在這個 repo 裡。

**加到主畫面之後，程式換了會自己更新。**
全螢幕模式沒有網址列也沒有重整鍵，所以頁面自己去問 server「程式換了沒」——
從別的 App 切回來的時候問一次，一直開著的話每十分鐘問一次。
換了就重載，**但正在打字或對話框開著的時候不硬來**，改成跳一條可以點的提示。
指紋只看程式檔不看資料：跟著資料一起變的話，記一筆帳就會叫你重新載入。

**自己用的時候，資料存成 JSON 檔而不是 localStorage。**
清一次瀏覽器快取，記帳資料就沒了。而且檔案放在家目錄，別的工具才讀得到——
可以直接叫 Claude Code 幫你記一筆、算這個月的餐費。鎖在瀏覽器裡的資料誰都碰不到。

**server 預設只綁 127.0.0.1。**
記帳和備忘不該是同一個網路上誰都讀得到的東西。要在手機上看的話，
用 `--host` 指定一個私有網路（例如 Tailscale）的位址；程式會直接拒絕綁 `0.0.0.0`。

**存檔先寫暫存檔再 rename。**
中途斷電的話，舊檔還是完整的舊檔，不會變成寫到一半的壞 JSON。
每次覆蓋前留一份備份，保留最近 20 份。

**讀不出來的檔案不當成空的。**
當成空的話，畫面會顯示「你沒有任何帳目」，然後下一次存檔就把壞檔覆蓋成真的空檔。
所以讀取失敗會整頁停住並說明原因，一個字都不動。

**沒有任何相依套件。**
前端是原生 JS，圖表用 CSS 畫，圖示是手寫的 inline SVG；後端只用 Python 標準庫。
這種規模的東西拉一個圖表庫進來不划算，而且離線也要能開。

## 自己跑一份

線上版的資料存在瀏覽器裡，清快取就沒了。要長期用的話跑本機版：

```sh
git clone https://github.com/huang940818-art/starcal-dashboard.git
cd starcal-dashboard
./開.sh                    # 開 server 並打開瀏覽器
```

資料會存在 `~/星歷資料/*.json`（權限 700/600），**不在專案資料夾裡**。

```sh
python3 server.py --port 8787        # 換 port
python3 server.py --host 10.0.0.5    # 給同一個私有網路的手機看
STARCAL_DATA_DIR=/tmp/試玩 python3 server.py   # 換一個資料夾（測試用）
```

兩種模式都可以在右上角的徽章裡**匯出成一個檔案**，換裝置或備份就靠它。

## 測試

```sh
node --test 測試.mjs     # 19 條：算術
node 檢查.mjs            # 175 條：真的開瀏覽器點過一遍（桌機一輪、手機寬度一輪）
node 版本測試.mjs         # 6 條：自己更新用的版本指紋
```

`測試.mjs` 驗餘額、月結、分類統計、下次扣款、金額與日期格式化，
還有示範資料本身的一致性（帳目不能指到不存在的帳戶或分類）。
算錯了不會有任何地方報錯，只會安靜地給一個錯的數字，所以這些一定要測。

`檢查.mjs` 開一個 headless Chrome，把每個對話框、每個按鈕點過一遍，
**跑在臨時資料夾上**，每次從全空開始——因為真正會被踩到的 bug
（取消鍵按不動、預算打開是空的、頂欄塌掉）都是算術測試抓不到、
而且只在「全新使用者」的狀態下才出現的。

## 檔案

```
index.html        版面
css/style.css     配色（墨綠 #132019、星星黃 #F9D984）
js/store.js       資料層，本機 / 展示兩種模式都在這裡
js/data.js        匯出、匯入、清空
js/util.js        日期、金額、DOM 小工具
js/icons.js       手寫的 inline SVG 圖示
js/demo.js        展示模式的示範資料（固定亂數種子，每次打開都一樣）
js/money.js       記帳
js/todo.js        待辦
js/agenda.js      行程 ＋ 時間線（三個檢視的總入口、分類篩選）
js/calendar.js    月曆格子
js/timetable.js   課表／班表
js/themes.js      八組主題的完整配色
js/prefs.js       主題、磨砂玻璃、分類
js/update.js      程式換了就自己更新（只在本機模式）
js/memo.js        備忘
js/wall.js        想法牆
js/ke.js          額度那一塊（只在本機模式出現）
js/overview.js    總覽
js/app.js         啟動與分頁
server.py         本機資料服務（只用標準庫）
測試.mjs           算術測試
檢查.mjs           畫面與互動檢查
```

## 作者

杯子蛋糕好好吃 · huang940818@gmail.com

有問題或建議都可以開 [Issue](https://github.com/huang940818-art/starcal-dashboard/issues)
或直接寄信。

## 授權

MIT
