# 資料不在這個資料夾（在 ~/星歷資料/），這裡只是防呆——
# 萬一哪天有人把資料複製進來，也不會被 commit 上去。
*.json
!package.json
星歷資料/
備份/

.DS_Store

# 個人用的臨時頁面，不進公開 repo
私人-*.html

# Claude 的 worktree。**一定要擋。** git add -A 會把它當成
# 嵌入的 git repo 加進索引，推上去之後那一格是空的，
# clone 的人什麼都拿不到。
.claude/
